function checkout(){if(!cart.length)return;document.getElementById('cartView').style.display='none';document.getElementById('checkoutView').style.display='block';document.getElementById('cardSuccessView').style.display='none';document.getElementById('successView').style.display='none';document.getElementById('checkoutMsg').textContent='';document.getElementById('cardInfo').textContent=typeof CARD_NUMBER!=='undefined'?CARD_NUMBER:''}
async function submitCardTransfer(){const c=customerData();if(!c)return;const msg=document.getElementById('checkoutMsg');msg.textContent='در حال ثبت سفارش و آماده‌سازی دانلود...';try{const res=await fetch(API,{method:'POST',headers:{'Content-Type':'application/json','apikey':API_KEY,'Authorization':`Bearer ${API_KEY}`},body:JSON.stringify({action:'card_transfer',product_ids:cart.map(p=>p.id),customer_name:c.name,customer_mobile:c.mobile,customer_email:c.email})});const data=await res.json();if(!res.ok||!data.order_number||!data.token)throw new Error(data.error||'ثبت سفارش ناموفق بود.');document.getElementById('checkoutView').style.display='none';document.getElementById('cardSuccessView').style.display='block';document.getElementById('cardSuccessText').textContent=`سفارش شماره ${Number(data.order_number).toLocaleString('fa-IR')} ثبت شد. مبلغ ${fa(data.total_toman)} را کارت‌به‌کارت کن. لینک دانلود شما در ادامه آماده است.`;const box=document.getElementById('cardDownloads');box.innerHTML=data.links?.length?data.links.map(x=>`<a class="pay" style="display:block;text-decoration:none;text-align:center;margin-top:10px" href="${x.url}" target="_blank" rel="noopener">⬇️ دانلود ${x.name}</a>`).join(''):'<p>فایل محصول هنوز در انبار بارگذاری نشده است.</p>';sessionStorage.setItem('lastOrder',String(data.order_number));cart=[];updateCart()}catch(e){msg.textContent=e.message||'خطایی رخ داد. دوباره تلاش کن.'}}